# dependency from WhisperStreaming:
librosa

# for using Silero VAD with WhisperStreaming, torchaudio is needed
torchaudio

# from SimulWhisper
torch

# from Whisper
tqdm
tiktoken

# both Whisper and SimulWhisper
# triton is used optionally for CUDA acceleration in triton_ops.py (dtw_kernel, median_filter)
# with fallback implementations if triton fails. Core APIs (jit, language, JITFunction) are stable.
triton>=2.0.0


